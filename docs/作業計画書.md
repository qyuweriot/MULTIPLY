# 作業計画書：カードゲームのWeb実装

Claude Code 引き継ぎ用。ルール仕様は `カード効果テキスト.md` を参照（そちらが正典。矛盾したらそちらを優先）。

---

## 0. ゴール

- 2人対戦のターン制カードゲームを、静的サイトとして GitHub Pages に公開する
- 対戦形式：**ホットシート（同一端末で2人交互）** と **CPU対戦** の2モード
- すべて公開情報のゲームなので、秘匿画面や手札の裏面表示は不要
- バランス調整のため、**UIを介さずに大量自己対戦できるCLIシミュレータ**を同梱する

---

## 1. 設計方針（ここが最重要）

### 1-1. コアはUIから完全に分離した純関数にする

`src/core/` 配下は **DOM・React・ブラウザAPIに一切依存しない**。状態遷移は次の形に統一する。

```ts
applyMove(state: GameState, move: Move): GameState   // 非破壊。新しいstateを返す
legalMoves(state: GameState): Move[]                 // 対象選択まで含めて完全列挙
```

理由は3つ。

1. **CPUが先読みできる。** 探索は `applyMove` を繰り返すだけで実装できる
2. **バランス調整が高速に回る。** 同じコアをNode上で1万回叩ける
3. **テストが書ける。** 盤面を組んで合計数値を検証する単体テストが素直に書ける

state を破壊的に変更する実装にすると先読みが書けなくなるので、ここは妥協しないこと。immutable な更新を徹底する（構造が小さいのでコストは無視できる）。

### 1-2. 乱数はシード可能にして state に持たせる

`Math.random()` を直接呼ばない。PRNGの状態を `GameState` に含めることで、リプレイ・バグ再現・シミュレーションの再現性がすべて確保される。

```ts
// src/core/rng.ts
export type RngState = number;
export function nextRandom(s: RngState): [number, RngState] {
  let t = (s + 0x6D2B79F5) | 0;
  t = Math.imul(t ^ (t >>> 15), t | 1);
  t ^= t + Math.imul(t ^ (t >>> 7), t | 61);
  return [((t ^ (t >>> 14)) >>> 0) / 4294967296, t];
}
```

### 1-3. ゾーンの表示名は差し替え可能にする

内部識別子は `p0z0 / p0z1 / p1z0 / p1z1` に固定し、表示名は1ファイルに集約する。効果テキスト側もゾーン固有名を使わない設計になっているため、名前を変えても修正は表示層だけで済む。**Phase 8-A で確定した。**

```ts
// src/labels.ts
export const ZONE_LABELS = { z0: 'L', z1: 'R' } as const;
```

**プレイヤーの呼称は対戦相手モードに依存する。** 静的な定数にはできない。

| モード | 先攻 | 後攻 |
|---|---|---|
| 人間同士 | `Player1` | `Player2` |
| CPU 戦 | `Player` | `CPU`（`App.tsx` の `CPU_PLAYER = 1`） |

```ts
export function playerLabels(vsCpu: boolean): PlayerLabels
/** 「Player2-R」のような、持ち主つきのゾーン名 */
export function zoneName(key: ZoneKey, labels: PlayerLabels): string
```

`PLAYER_LABELS` という定数は置かない。置くと「モードを見ていない古い呼び出し」が気づかれずに生き残るので、**呼称が要る箇所は `labels` を受け取らせて型で洗い出す**。`App.tsx` が1つ作って props で配る（Context は使わない。渡す先は Board → Zone の2段が最長）。

### 1-4. CPUに山札の中身を見せない

山札の順序は非公開情報。CPUに `state` をそのまま渡すと**カンニングできてしまう**ので、思考ルーチンには「山札の残りカードの集合（順序なし）」だけを渡すビューを用意する。

```ts
export function visibleTo(state: GameState, p: PlayerId): PublicView
```

手札は両者公開なので、隠れているのは山札の順序だけ。

---

## 2. 技術選定

| 項目 | 選定 | 理由 |
|---|---|---|
| ビルド | Vite | Pages への静的出力が最短。設定は `base` 1行 |
| 言語 | TypeScript（strict） | 効果の分岐が多く、型で守る価値が高い |
| UI | React | 状態駆動の再描画と相性が良い。ただしコアには持ち込まない |
| スタイル | CSS Modules または素のCSS | 盤面は4ゾーン＋手札のみ。CSSフレームワークは不要 |
| テスト | Vitest | Viteと同一設定で動く |
| CI/CD | GitHub Actions | Pages への公式デプロイフロー |

外部の状態管理ライブラリ・UIライブラリは入れない。`useState` で `GameState` を1つ持てば足りる。

---

## 3. リポジトリ構成

```
/
├─ src/
│  ├─ core/                   ← DOM非依存。ここが本体
│  │  ├─ cards.ts             カード定義データ（12種）
│  │  ├─ types.ts             型定義
│  │  ├─ rng.ts               シード可能PRNG
│  │  ├─ setup.ts             山札生成・シャッフル・初期配布
│  │  ├─ value.ts             数値計算エンジン（4層）★最重要
│  │  ├─ zone.ts              ゾーンロック管理
│  │  ├─ moves.ts             合法手の列挙
│  │  ├─ apply.ts             applyMove と設置時効果
│  │  └─ score.ts             得点・勝敗判定
│  ├─ ai/
│  │  ├─ view.ts              CPU向け情報制限ビュー
│  │  ├─ evaluate.ts          評価関数
│  │  └─ search.ts            思考ルーチン（難易度3段）
│  ├─ ui/
│  │  ├─ App.tsx
│  │  ├─ Board.tsx            4ゾーン表示
│  │  ├─ Hand.tsx             手札（両者公開）
│  │  ├─ TargetPicker.tsx     渦潮・刺創の対象選択
│  │  ├─ Log.tsx              行動ログ
│  │  └─ Result.tsx           結果画面
│  ├─ labels.ts               ゾーン表示名など
│  └─ main.tsx
├─ sim/
│  └─ run.ts                  CLIシミュレータ
├─ tests/
│  ├─ value.test.ts           ★ゴールデンテスト（§9）
│  ├─ zone.test.ts
│  └─ flow.test.ts
├─ docs/
│  ├─ カード効果テキスト.md    ルール正典
│  └─ 作業計画書.md            本ファイル
├─ .github/workflows/deploy.yml
└─ vite.config.ts
```

---

## 4. データモデル

```ts
// src/core/types.ts
export type PlayerId = 0 | 1;
export type ZoneKey = 'p0z0' | 'p0z1' | 'p1z0' | 'p1z1';
export const ALL_ZONES: ZoneKey[] = ['p0z0', 'p0z1', 'p1z0', 'p1z1'];

export type CardId =
  | 'gekko' | 'soyoku' | 'heigen' | 'hanmo' | 'hyozan' | 'uzushio'
  | 'shippu' | 'shiso' | 'kagero' | 'dangai' | 'ashikase' | 'horaana';

export interface CardDef {
  id: CardId;
  name: string;       // '月光'
  reading: string;    // 'げっこう'
  baseValue: number;  // 洞穴は0、足枷は-3
  priority: 0 | 1 | 2 | 3;
  hasOnPlace: boolean;
  copies: number;
  text: string;       // 表示用テキスト
}

export interface CardInstance {
  uid: number;        // インスタンス固有。同名カードの区別に必須
  defId: CardId;
}

export interface ZoneState {
  cards: CardInstance[];          // 氷山による制限は「氷山がこのゾーンにあるか」で判定する
}

export interface GameState {
  zones: Record<ZoneKey, ZoneState>;
  hands: [CardInstance[], CardInstance[]];
  deck: CardInstance[];           // 先頭がトップ
  discard: CardInstance[];
  turn: number;                   // 1〜14
  current: PlayerId;
  forcedZone: ZoneKey | null;     // 繁茂。構造上つねに0個か1個
  rng: RngState;
  log: LogEntry[];
  phase: 'playing' | 'finished';
}

export interface Move {
  cardUid: number;
  zone: ZoneKey;
  targetUid?: number;   // 渦潮・刺創の対象
  moveTo?: ZoneKey;     // 渦潮の移動先
  discardOnly?: boolean; // 安全弁（合法手0件時）
}
```

**`uid` は必須。** 同名カードが最大4枚あるため、カード名だけでは対象を一意に指定できない。

---

## 5. 数値計算エンジン（★最重要・最初に作る）

`優先度` は **数字が大きいものから先に処理**。先の層で確定した値は後の層で上書きしない。

```ts
// src/core/value.ts
export function cardValues(state: GameState, key: ZoneKey): Map<number, number> {
  const zone = state.zones[key];
  const has = (id: CardId) => zone.cards.some(c => c.defId === id);
  const out = new Map<number, number>();

  // 層1（優先度3）陽炎：すべての数値変動を無効化 → 本来の数値をそのまま返す
  if (has('kagero')) {
    for (const c of zone.cards) out.set(c.uid, base(c));
    return out;
  }

  // 層2（優先度2）洞穴：合計値を上書きするため個別値は意味を持たない
  //   → zoneTotal 側で早期returnする。ここでは本来の数値を入れておく
  if (has('horaana')) {
    for (const c of zone.cards) out.set(c.uid, base(c));
    return out;
  }

  // 層3（優先度1）月光：本来の数値が1なら3、それ以外は0。値はここで確定
  if (has('gekko')) {
    for (const c of zone.cards) out.set(c.uid, base(c) === 1 ? 3 : 0);
    return out;   // 確定済みなので層4は処理しない
  }

  // 層4（優先度0）個別の自己条件
  const n = zone.cards.length;
  const paired = soyokuPaired(state, ownerOf(key));
  for (const c of zone.cards) {
    let v = base(c);
    if (c.defId === 'soyoku'   && paired) v = 3;
    if (c.defId === 'dangai'   && n >= 3) v = 0;
    if (c.defId === 'ashikase' && n >= 5) v = 0;
    out.set(c.uid, v);
  }
  return out;
}

export function zoneTotal(state: GameState, key: ZoneKey): number {
  const zone = state.zones[key];
  const has = (id: CardId) => zone.cards.some(c => c.defId === id);
  if (!has('kagero') && has('horaana')) return 5;   // 層2。陽炎がなければ合計5固定
  return [...cardValues(state, key).values()].reduce((a, b) => a + b, 0);
}

// 双翼の成立判定：持ち主の2ゾーン両方に双翼が「存在するか」。
// 陽炎は数値変動を止めるだけで存在を隠さないので、陽炎のあるゾーンの双翼も数える。
function soyokuPaired(state: GameState, owner: PlayerId): boolean {
  return (['z0', 'z1'] as const).every(z =>
    state.zones[`p${owner}${z}` as ZoneKey].cards.some(c => c.defId === 'soyoku'));
}

export function score(state: GameState, p: PlayerId): number {
  return zoneTotal(state, `p${p}z0` as ZoneKey) * zoneTotal(state, `p${p}z1` as ZoneKey);
}
```

**再計算はキャッシュしない。** 盤面が変わるたびに全層を再計算する。カードは最大14枚なので性能上の問題は起きない。差分更新は不整合の温床になるので禁止。

---

## 6. ゾーンの設置制限（氷山）

氷山の効果は「**このゾーンには、本来の数値が2のカードしか置くことができない**」。
判定に必要なのは「このゾーンに氷山があるか」だけなので、枚数を数える状態は持たない。

```ts
// src/core/zone.ts
export const HYOZAN_ALLOWED_VALUE = 2;

export function onEnter(zone: ZoneState, card: CardInstance): ZoneState {
  return { cards: [...zone.cards, card] };
}

export function onLeave(zone: ZoneState, card: CardInstance): ZoneState {
  return { cards: zone.cards.filter(c => c.uid !== card.uid) };
}

export function isRestricted(zone: ZoneState): boolean {
  return zone.cards.some(c => c.defId === 'hyozan');
}

export function canPlace(zone: ZoneState, card: CardInstance): boolean {
  return !isRestricted(zone) || base(card) === HYOZAN_ALLOWED_VALUE;
}
```

- **判定は本来の数値**（正典 §2）。効果で変動した後の値は見ない
- 通せるのは氷山・渦潮・疾風（各3枚）。**氷山自身も2なので重ねられる**。枚数の上限はない
- 氷山が離れれば制限も消える。別の氷山が残っていれば維持される
- **「制限中のゾーンには必ず氷山がある」が保証される。** 氷山は3枚なので最大3ゾーン、何でも置けるゾーンが必ず1つ以上残る
- §1-1 に従い純関数にする（サンプルの破壊的シグネチャは採用しない）

---

## 7. ターン進行

```
1. phase !== 'playing' なら終了
2. 山札トップを1枚引いて current の手札に加える
3. forcedZone を capturedForce に退避し、forcedZone = null にする   ← ★
4. legalMoves(state) を列挙（capturedForce があり、かつそのゾーンにそのカードを置けるなら設置先を限定）
5. 手を選択（人間 = UI操作 / CPU = search）
6. カードをゾーンに設置（onEnter）
7. 設置時効果を解決（繁茂ならここで forcedZone が新たにセットされる）
8. turn++、current を交代
9. turn > 14 なら phase = 'finished'、得点計算
```

**ステップ3を必ず先に行う。** 先に `forcedZone` をクリアしないと、そのターンに繁茂を置いた場合に自分でセットした制約を自分で消してしまう。

### 設置時効果の実装

| カード | 処理 |
|---|---|
| 平原 | 手札全部を山札に戻す → シャッフル（rngを更新）→ 2枚引く |
| 繁茂 | `forcedZone = 設置したゾーン` |
| 渦潮 | `targetUid` のカードを `moveTo` へ（onLeave → onEnter）。設置時効果は再発動しない |
| 疾風 | `hands[0]` と `hands[1]` を交換 |
| 刺創 | `targetUid` のカードを `discard` へ（onLeave） |

氷山・陽炎・月光・双翼・断崖・足枷・洞穴は常在効果なので、設置時に特別な処理は不要（`zoneTotal` が毎回計算する）。

---

## 8. 合法手の列挙

```ts
export function legalMoves(state: GameState, force: ZoneKey | null): Move[] {
  const hand = state.hands[state.current];

  const moves: Move[] = [];
  for (const card of hand) {
    // 氷山の制限はカードごとに効くので、候補は手札1枚ずつ出す
    const open = ALL_ZONES.filter(z => canPlace(state.zones[z], card));
    // 繁茂の強制。ただし強制先にそのカードを置けなければ不発 → 自由に置ける
    const zones = (force && canPlace(state.zones[force], card)) ? [force] : open;

    for (const zone of zones) {
      const others = state.zones[zone].cards;   // 設置前の状態。自身は含まれない
      if (defOf(card).id === 'shiso') {
        if (others.length === 0) moves.push({ cardUid: card.uid, zone });   // 不発
        else for (const t of others) moves.push({ cardUid: card.uid, zone, targetUid: t.uid });
      } else if (defOf(card).id === 'uzushio') {
        // 移動先の可否は「移動するカード」で決まる
        let any = false;
        for (const t of others) for (const d of ALL_ZONES) {
          if (d === zone || !canPlace(state.zones[d], t)) continue;
          moves.push({ cardUid: card.uid, zone, targetUid: t.uid, moveTo: d });
          any = true;
        }
        if (!any) moves.push({ cardUid: card.uid, zone });   // 不発
      } else {
        moves.push({ cardUid: card.uid, zone });
      }
    }
  }

  // 安全弁：合法手が0件なら手札を1枚捨ててターン終了
  if (moves.length === 0) return hand.map(c => ({ cardUid: c.uid, zone: 'p0z0' as ZoneKey, discardOnly: true }));
  return moves;
}
```

- **渦潮・刺創は自身を対象に選べない。** 設置前のゾーン内容を対象候補にすれば自然に除外される
- 渦潮の移動先は元ゾーンと、**移動するカードを置けないゾーン**を除く
- 対象がいない場合は不発（カードは数値として残る）
- 安全弁はルール上到達しないが、カード追加時やバグ時にゲームがフリーズしないための最終防衛ライン。**CPU側も合法手0件の分岐を必ず持たせる**

---

## 9. テスト（先に書く）

数値計算エンジンは分岐が絡み合うので、実装より先にこのテストを書く。すべて `tests/value.test.ts`。

| # | ゾーンの内容 | 期待値 | 検証内容 |
|---|---|---|---|
| 1 | 月光 + 断崖（2枚のみ） | 合計 **0** | 月光が本来の数値3を見て0に確定。断崖の自己条件（3枚未満なので3のまま）で上書きされない |
| 2 | 月光 + 双翼（他ゾーンにも双翼） | 合計 **3** | 月光は本来の数値1を見て3にする。双翼の3と競合しない |
| 3 | 月光 + 足枷 | 合計 **0** | 月光が足枷を解除（-3→0） |
| 4 | 陽炎 + 月光 + 断崖 | 合計 **6** | 陽炎が全変動を無効化（3+0+3） |
| 5 | 陽炎 + 洞穴 + 断崖 | 合計 **6** | 陽炎で洞穴が無効化され、数値0のカードとして通常計算（3+0+3） |
| 6 | 洞穴 + 月光 + 刺創 + 陽炎なし | 合計 **5** | 洞穴が合計を上書き。月光は無意味 |
| 7 | 洞穴 + 洞穴 | 合計 **5** | 同ゾーン2枚でも5 |
| 8 | 双翼2枚が片方のゾーンのみ | 合計 **2** | 両ゾーンに必要。片寄せでは成立しない |
| 9 | 陽炎+双翼のゾーン／双翼のみのゾーン | 前者の双翼 **1**、後者 **3** | 陽炎は存在判定を妨げない |
| 10 | 断崖 + 平原 + 繁茂 | 合計 **2** | 断崖自身を数えて3枚 → 断崖0 |
| 11 | 足枷 + 任意4枚 | 足枷が **0** | 足枷自身を数えて5枚 |
| 12 | 双翼×2（両ゾーン）+ 3枚目の双翼 | 3枚すべて **3** | 3枚目以降も3 |

`tests/zone.test.ts`

| # | 手順 | 期待 |
|---|---|---|
| 1 | 氷山ゾーンに数値2（氷山・渦潮・疾風） | 置ける |
| 2 | 氷山ゾーンに数値3・1・0・-3 | 置けない |
| 3 | 氷山ゾーンに氷山 | 置ける（自身も2）。枚数の上限はない |
| 4 | 月光で0になっている断崖を氷山ゾーンへ | 「本来3」なので置けない |
| 5 | 氷山がゾーンを離れる | 制限解除。氷山2枚で1枚なら維持 |
| 6 | 氷山3枚を3ゾーンに配置 | 4つ目のゾーンは何でも置ける |
| 7 | 渦潮の移動先 | 移動するカードの本来の数値で判定される |

`tests/flow.test.ts`

- 14ターンで必ず `finished` になる
- 全ターンで `legalMoves` が1件以上返る（シード100通りで検証）
- 繁茂の強制が次の1ターンのみ有効で、そのターン終了後に消える
- 繁茂を置いたターンに自分の制約が消えていない（§7ステップ3の検証）
- 同一シードで2回実行して同じ棋譜になる（再現性）

---

## 10. CPU

```ts
export type Difficulty = 'easy' | 'normal' | 'hard';
export function chooseMove(view: PublicView, d: Difficulty): Move;
```

| 難易度 | 方式 |
|---|---|
| easy | 合法手からランダム |
| normal | 1手先読み。自分の手を適用した直後の評価値が最大の手 |
| hard | 2手先読み（自分の手 → 相手の最善応手）のミニマックス |

分岐数は概算で「手札2〜3枚 × 開いているゾーン最大4 × 対象組合せ」＝**多くて数十手**。2手先読みでも数千ノードなので、探索はブラウザ上で瞬時に終わる。

### 評価関数

```ts
export function evaluate(state: GameState, me: PlayerId): number {
  const opp = (1 - me) as PlayerId;
  let v = score(state, me) - score(state, opp);

  if (state.phase !== 'finished') {
    // 得点が積になるため、片方のゾーンが0だと総得点が0になる。終盤ほど強く警戒する
    const w = state.turn / 14;
    for (const z of ['z0', 'z1'] as const) {
      if (zoneTotal(state, `p${me}z${z.slice(1)}` as ZoneKey) <= 0) v -= 20 * w;
    }
    // 積は均衡させたほうが大きい（6×6=36 > 10×2=20）
    const a = zoneTotal(state, `p${me}z0`), b = zoneTotal(state, `p${me}z1`);
    v -= Math.abs(a - b) * 0.5;
  }
  return v;
}
```

相手の山札トップは不明なため、`hard` では相手の応手を読む際にドローを無視するか、残り山札から数枚サンプリングして平均を取る。まずは**ドロー無視**で実装し、弱ければサンプリングに拡張する。

---

## 11. バランス調整用シミュレータ

```
npm run sim -- --games 10000 --p0 hard --p1 hard --seed 42
```

コアが純関数なのでUIを介さず回せる。出力する指標と目標値。

| 指標 | 目標 | 何を見るか |
|---|---|---|
| 先攻勝率 | 50 ± 3% | 先攻後攻の有利不利。差があれば初期手札枚数などで補正 |
| 同点率 | 5% 未満 | 積が一致する頻度。同点は先攻の勝ちなので勝敗には出ない診断値 |
| 0点決着率 | 5% 未満 | 片方のゾーンが0で終わる「事故」の頻度 |
| 平均スコア | 25〜35 | 設計上の想定は29 |
| **最終ターン逆転率** | 30% 未満 | **最重要。** 高いと「最後に置いた方が勝つ」ゲームになっている |
| カード別勝率貢献 | 極端な偏りがないこと | 使用したゲームの勝率。月光・洞穴・陽炎を重点監視 |
| カード別の設置先分布 | — | 自ゾーン／相手ゾーンの比率。相手ゾーンに一度も置かれないカードは妨害札として機能していない |

**最終ターン逆転率が高い場合の対処は既に検討済み**（月光・洞穴を設置時1回のみの効果に変更する案）。まずは現行ルールで計測し、数値を見て判断する。

引き分けは存在しない（正典 §1。同点は先攻の勝ち）。「最終ターン逆転率」の判定にも同じタイブレークを使う — 並んでいる盤面は「先攻がリード」として数える。`sim/run.ts` の `leaderOf()` がそれ。

---

## 12. GitHub Pages デプロイ

`vite.config.ts`

```ts
export default defineConfig({
  base: '/<リポジトリ名>/',   // ← ここを実際のリポジトリ名に。忘れるとアセット404になる
  plugins: [react()],
});
```

`.github/workflows/deploy.yml`

```yaml
name: deploy
on:
  push:
    branches: [main]
permissions:
  contents: read
  pages: write
  id-token: write
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with: { node-version: 20, cache: npm }
      - run: npm ci
      - run: npm test
      - run: npm run build
      - uses: actions/upload-pages-artifact@v3
        with: { path: dist }
  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment: github-pages
    steps:
      - uses: actions/deploy-pages@v4
```

リポジトリ設定 → Pages → Source を **GitHub Actions** に変更する。SPAでルーティングは使わないので `404.html` の対応は不要。

---

## 13. フェーズ分割

各フェーズは独立してコミット可能。**Phase 2 を飛ばして先に進まないこと。**

### Phase 0：セットアップ
- Vite + React + TypeScript（strict）でプロジェクト作成
- Vitest 導入、`vite.config.ts` の `base` 設定
- deploy.yml を置き、空のページが Pages で表示されるところまで確認
- 完了条件：公開URLで「Hello」が見える

### Phase 1：カードデータと型
- `types.ts`、`cards.ts`（12種の定義と枚数）、`rng.ts`、`setup.ts`
- 完了条件：30枚の山札が生成され、シード固定で同じ順序になる

### Phase 2：数値計算エンジン ★最重要
- `value.ts`、`zone.ts`
- **§9のゴールデンテストを先に書き、全パスさせる**
- 完了条件：`tests/value.test.ts` と `tests/zone.test.ts` が全緑

### Phase 3：ゲーム進行
- `moves.ts`、`apply.ts`、`score.ts`。設置時効果を除いた骨組み
- 完了条件：ランダム同士で14ターン完走し、勝敗が出る（UIなし、Nodeで確認）

### Phase 4：設置時効果
- 平原・繁茂・渦潮・疾風・刺創の5種を実装
- 完了条件：`tests/flow.test.ts` が全緑

### Phase 5：UI
- 4ゾーンの盤面、両者の手札（公開）、対象選択、行動ログ、結果画面
- 各カードの現在の数値と、本来の数値との差分を表示する（**効果で数値が変わったことが目で見えないと遊べない**）
- ゾーン合計と得点をリアルタイム表示
- 完了条件：ホットシートで1ゲーム通して遊べる

### Phase 6：CPU
- `view.ts`、`evaluate.ts`、`search.ts`。難易度3段
- 完了条件：hard が easy に対して勝率80%以上

### Phase 7：シミュレータとバランス調整
- `sim/run.ts` と §11 の指標出力
- 1万戦回して指標を確認し、必要なら枚数・効果を調整
- 完了条件：§11 の目標値を満たす

#### 先攻勝率と、引き分けの廃止

変更前は hard 対 hard の1万戦で 42.2%。

各ターンの着手直後にどちらが勝つ盤面かを測ると、打った側が優位を取り返す**完全なシーソー**になっている。**手番そのものはほぼ公平**で、偶数ターンで終わるために後攻の振り上げで打ち切られているだけ。最終手が得点差を動かす量は平均 −11.7 点、84.3% の試合で後攻に有利へ動く。

構造由来なので、演出・AI・カード調整では動かない。先攻への補償として、**引き分けを廃止して同点は先攻の勝ちとした**（正典 §1）。

| | 変更前 | 変更後 |
|---|---|---|
| 先攻勝率（hard） | 41.8% | **45.4%** |
| 先攻勝率（normal） | 47.5% | **50.5%** ✓ |
| 先攻勝率（easy） | 47.6% | **50.2%** ✓ |
| 最終ターン逆転率（hard） | 23.8% | **20.6%** |

hard 同士では目標域（47〜53%）に 1.6pt 届かないが、**normal 以下では達成**する。人間が遊ぶ範囲は概ねこちらで、hard の残差は「最終手の価値を正確に読み切れる打ち手ほど後攻有利を取り切れる」という戦略的性質そのもの。ここは目標値の側を見直す扱いとし、**先攻勝率は厳密には追わない**。

タイブレークは**最終ターン逆転率の判定にも同じものを使う**（`sim/run.ts` の `leaderOf()`）。並んだ盤面を「リードなし」と数えると、実際のルールとズレる。

#### カード別勝率貢献の測り方（交絡に注意）

`sim/run.ts` のカード別勝率は先攻・後攻をまとめて数えている。後攻の勝率が 54% ある以上、**「後攻に多く使われた札」というだけで勝率が上がって見える**。調整の判断には、先攻・後攻それぞれの基準勝率との差を採ること。

#### 平原・足枷の調整（実測）

hard 対 hard の1万戦、交絡を外した基準差（先攻／後攻の平均）：

| 案 | 平原 | 足枷 | 判定 |
|---|---|---|---|
| 変更前 | −6.6 | −8.7（12種中最下位） | — |
| **足枷 −3 のみ** | −6.2 | **−5.5** | **採用** |
| 平原「両者が引き直す」のみ | **−7.9** | −9.3 | 不採用 |
| 両方 | −7.7 | −4.0 | — |

- **足枷 −3 を採用した。** 最下位を脱し、自ゾーン率も 4.8% → 2.4% に下がって「相手にだけ置く妨害専用札」という性格がはっきりした
- **平原「両者が2枚引き直す」は逆効果だったので元に戻した。** 自分の悪い手札を直せる代わりに相手の悪い手札まで直してしまい、1の札を1枚使う費用に見合わない。相手ゾーンへの設置率も 29.3% → 26.1% と、妨害札としても機能していなかった
- **採用後（足枷 −3 のみ、引き分け廃止も込み）の §11 の5指標：** 先攻 45.4% ／ 同点 3.6% ／ 0点 2.9% ／ 平均 30.5 ／ 逆転 20.6%（先攻勝率以外はすべて達成）
- 平原に手を入れるなら、**相手だけが引き直す**か**自分2枚・相手1枚**のような非対称案でないと「相手を助けてしまう」問題は解けない

##### 測定の注意（確認済み・問題なし）

平原が相手の手札まで書き換えると、hard の `deepValue` は相手の応手を「書き換わった後の手札」から列挙する。`view.ts` の `canonicalize` が山札を定義順に並べ替えているため一見バイアスがかかりそうだが、**`resolveOnPlace` が `state.rng` で実際にシャッフルしてから配るので、AI が見る引き直し後の手札は正しい残りカードからの無作為標本になる**。設置回数も 13874 → 14576 と落ちておらず、上の結果は探索の歪みではない。

### Phase 9：演出
Phase 8 の「簡単な演出」から独立させた。UI だけで完結し、`src/core` には手を入れていない。

- 9-A：演出の土台と設置時効果5種
  - `ui/effects.ts`（`LogEntry` と着手前後の `GameState` から演出イベントを組み立てる純関数）
  - `ui/useBoardTransition.ts`（FLIP。カードの移動・出現・消滅）
  - `ui/EffectLayer.tsx`（発動カットイン）、`ui/effects.css`（12種の固有モーション）
  - あわせてレイアウトの伸縮を潰した。盤面が伸び縮みすると FLIP が測る基準が変わり、
    無関係なカードまで動いて見える（「ここに置く」ボタン41px・空ゾーンの1枚目67px・
    決着パネル128px・「手番」バッジ1.6px を、いずれも領域確保で解決）
- 9-B：常在効果7種の可視化
  - `ui/passives.ts`。カード枠の色（発動中／無効化／条件未達）とゾーンバッジ
- 9-C：渦潮の対象をドラッグ&ドロップで移動先へ運べるようにする
- 完了条件：12種すべてのカットインが例外なく再生され、着手のたびに盤面がずれないこと

#### 9-D：効果音

**音声ファイルは1つも置かない。すべて Web Audio API で合成する。** 依存が増えず、
ライセンスの心配がなく、配布サイズも増えない（カード画像だけで既に約1.8MB ある）。
12種の固有音もデータとして書けるぶんファイルより手軽になる。

**BGM は入れない**（判断済み）。長考するゲームなので、鳴り続けるものは邪魔になる。

| ファイル | 役割 |
|---|---|
| `ui/soundSpec.ts` | 音の設計図。**Web Audio にも DOM にも触らない純関数とデータ**なので node のテストで固定できる |
| `ui/sound.ts` | Web Audio エンジン。`Sound` を受け取って鳴らすだけ |
| `ui/useSound.ts` | `multiply:sound`（既定 ON）の読み書きと再生関数 |

- 音は `EffectEvent` に乗せる。演出（9-A〜C）が組み立てた同じイベントを使うので、
  `commit()` に1行足すだけで12種すべてに固有音が付く
- 12種はカットインのモチーフ（`effects.css`）と対応させる。刺創は鋭いノイズ、
  氷山は硬質なベル、渦潮はバンドパスの周回、繁茂は上昇アルペジオ、など
- **不発・捨札は同じカードの音を湿らせる**（`dampen`）。別の音にすると「何が起きたか」と
  「うまくいかなかった」が耳の中で切り離れる
- **音は演出トグルとも `prefers-reduced-motion` とも独立**。視差を減らしたい人と
  音を消したい人は別なので、トップバーに「音」を別に置く
- **自動再生規制**：`AudioContext` は最初のユーザー操作まで `suspended`。
  最初の `pointerdown` / `keydown` を一度だけ拾って `resume()` する

データ側で守っている不変条件（`tests/sound.test.ts` が固定）：

- 12種の音が互いに異なる（カットインは12種あるのに音は使い回し、を防ぐ）
- どの音も総尺 1.5 秒以内（次の操作に被らない）
- 同時に鳴るゲインの合計が 1.0 以下（クリップして歪まない）

##### 検証で分かったこと

- **`AudioParam.value` は当てにならない。** `setTargetAtTime` の最中に読むと、同じ
  状態でも 0 と 0.5 のどちらも返ってくる（ヘッドレスには音声デバイスが無く、
  オーディオスレッドの進み方が普通と違う）。実機検査は「ノードが作られたか」で見る
- **退場ゴーストにクラスが残る。** `useBoardTransition` は消えるカードの実 DOM ノードを
  `.fx-ghosts` へ移すので、捨てられたカードにも `.card--targetable` が付いたままになる。
  画面上はクリックできない（`pointer-events: none`）が、`el.click()` は通る。
  **自動操作するときは `.fx-ghosts` の中を必ず除外すること**

### Phase 8：仕上げ
- 8-A：呼称の確定（済）。ゾーンは `L` / `R`、プレイヤーは人間戦 `Player1` / `Player2`、
  CPU 戦 `Player` / `CPU`。持ち主つきは `Player2-R`（詳細は §1-3）
- ルール説明画面、レスポンシブ対応
- 盤面が1画面に収まらない（1440×1800 で全体 1531px）。
  トップバーも「対戦相手・演出・音」と増えたので、あわせて整理する

---

## 14. 引き継ぎ時の注意（間違えやすい順）

1. **`forcedZone` のクリアは手の選択より前**。でないと繁茂を置いたターンに自分の制約を自分で消す
2. **層の処理順は優先度の降順**（陽炎3 → 洞穴2 → 月光1 → 自己条件0）。**先に確定した値は後の層で上書きしない**
3. **月光は「本来の数値」を見る**。効果で変動した後の値ではない
4. **陽炎は味方の強化も止める**（双翼の1→3も無効）。バグではなく仕様
5. **氷山がゾーンを離れたら制限解除**。「制限中のゾーンには必ず氷山がある」が詰み防止の要
6. **渦潮・刺創は自身を対象に取れない**。設置前のゾーン内容を候補にすれば自然に除外される
7. **渦潮で移動したカードの設置時効果は再発動しない**
8. **相手ゾーンに置いたカードは相手の得点になる**。効果文の「相手」は使用者から見た相手、数値の集計は持ち主基準
9. **CPUに山札の順序を渡さない**
10. **`Math.random()` を直接呼ばない**

---

## 15. 未決定事項

- **ゾーンの呼称**（Phase 8で確定。`labels.ts` の1行変更で反映される）
**カードの効果と枚数は確定した。** 以下は見送った案の記録で、蒸し返す予定はない。

- ~~**月光・洞穴を設置時1回のみの効果に変更するか**~~ — 最終ターン逆転率 20.6% で目標内。**変更不要**
- ~~**陽炎の枚数**~~（現在2枚）— 基準差 +3.9〜+4.6 と上位だが、**現状で確定**
- ~~**先攻勝率の補正**~~ — 引き分けの廃止で 45.4%（normal 以下は達成）。**これ以上は追わない**（§13 Phase 7）
- ~~**平原の効果**~~ — 「両者が2枚引き直す」は実測で逆効果だったため元に戻した（§13 Phase 7）。**現状で確定**
- ~~**BGM**~~ — **入れない**と判断した（§13 Phase 9-D）。長考するゲームなので鳴り続けるものは邪魔になる
- ~~**効果音**~~ — Phase 9-D で実装済み。Web Audio で生成、音声ファイルなし
- 演出の尺（カットイン2000ms・盤面1200ms。トップバーの「演出」トグルで OFF にでき、
  `prefers-reduced-motion` でも無効になる）

**残っているのは Phase 8 だけ。**
